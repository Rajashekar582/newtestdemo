openapi: 3.0.3
info:
  title: Sample API
  description: This is a demo API for Spring Boot project
  version: "1.0.0"

paths:
  /hello:
    get:
      summary: Returns a hello message
      responses:
        '200':
          description: Successful response
          content:
            text/plain:
              schema:
                type: string